# Copy this to .env.local for local dev (already works without it)
# For production — set this in Netlify dashboard under Site Settings > Environment Variables
VITE_SERVER_URL=https://your-app-name.railway.app
